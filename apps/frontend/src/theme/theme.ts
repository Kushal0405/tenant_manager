import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: { main: "#1e3a5f" },
    secondary: { main: "#2e7d32" },
  },
  shape: {
    borderRadius: 8,
  },
});
